server:
  port: 8080
  servlet:
    context-path: /omnio
spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://omnio-messaging-db/omnio-messaging-db
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

jwt:
  token:
    secret: K02tUsz+cHDQ5iu39RyVcq9J7eRrLCcGxrNFZeURihQ=
    issuer: ms-auth
